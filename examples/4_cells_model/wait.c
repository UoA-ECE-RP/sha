 #include "wait.h"

 void waitFor (void) {
 	int i_timer=0, j_timer=0;
    //retTime = time(0) + secs;     // Get finishing time.
    for (i_timer ; i_timer<10000 ; i_timer ++){
      for (j_timer ; j_timer<10000 ; j_timer ++) {}}   // Loop until it arrives.
 }